{
  keywords: ["orden-tab", "ordenar-tab", "orden-grupos", "ordenar-grupos", "sort-groups", "sorting-groups"]
  displayName: "📋 ordenar-tab"

  message {
    summary: "Como ordenar grupos en el tab utilizando el plugin TAB."
    content: """
El proceso de ordenar los grupos en el plugin TAB se configura en `config.yml` dentro de la sección de `scoreboard-teams` > `sorting-types`

Por defecto, solo se proporciona un ejemplo con algunos rangos preconfigurados:
```yaml
  sorting-types:
    - "GROUPS:owner,admin,mod,default"
    - "PLACEHOLDER_A_TO_Z:%player%"
```

Este método ordena a los jugadores de A a Z y de "owner" a "default".
Para personalizar el orden, cambia los nombres de los grupos en tu servidor siguiendo el formato: GROUPS:owner,admin,mod,vip,vip+,default, etc.
Si no sabes el nombre de tu grupo, usa el comando /tab debug <player>.
Luego, usa /tab reload para aplicar los cambios.

Para más detalles, consulta la [wiki](<https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Sorting-players-in-tablist>) del plugin.
"""
  }
}
