{
  keywords: ["orden-tab", "ordenar-tab", "orden-grupos", "ordenar-grupos", "sort-groups", "sorting-groups", "tab"]
  displayName: "📋 ordenar-tab"

  message {
    summary: "Cómo ordenar grupos en TAB."
    content: """
Para ordenar los grupos en TAB, entra a su `config.yml`, en la sección de `scoreboard-teams` > `sorting-types`.
La config por defecto ordena a los jugadores de A a Z y de "owner" a "default":
```yaml
  sorting-types:
    - "GROUPS:owner,admin,mod,default"
    - "PLACEHOLDER_A_TO_Z:%player%"
```
Para cambiar el orden de grupos:
1. Cambia los nombres en la config a los que tengas en LuckPerms, por ejemplo `GROUPS:primerGrupo,segundoGrupo,...,default`.
  - Para saber el grupo en el que TAB piensa que está alguien, usa `/tab debug <nombre>`.
2. Usa `/tab reload` para aplicar los cambios.
Para más detalles, consulta la [wiki](<https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Sorting-players-in-tablist>) del plugin.
"""
  }
}
