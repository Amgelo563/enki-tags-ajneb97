{
  keywords: ["verbose"]
  displayName: "📢 verbose"

  message {
    summary: "Información sobre el modo Verbose"
    embeds: [{
      include "../__plantillas__/embed.conf"
      description: """
El modo Verbose de LuckPerms te permite ver los permisos siendo revisados en tiempo real. Útil por si quieres saber el nombre de un permiso que no encuentras.
**__Activar el modo Verbose__**
1. Usa `/lp verbose on [filtro]`. En el filtro puedes poner el nombre de un jugador, un permiso, etc. [Más información sobre esto en la wiki.](<https://luckperms.net/wiki/Verbose#filters>)
2. Haz lo que active el revisado del permiso que no conoces (ejecutar un comando, hacer algo en un menú, etc).
3. Cuando el permiso sea revisado, recibirás un mensaje en chat con información sobre el permiso.
4. Usa `/lp verbose off` para apagar el modo Verbose.
**__Usar el modo Verbose para ver el permiso de un comando__**
Usa `/lp verbose command <jugador> <comando>`. Esto activará el verbose para el jugador mandado, forzará a que use el comando y te mostrará los permisos que se revisaron.
"""
    }]
    buttons: [{
      type: "url"
      label: "Ver en la wiki"
      emoji: "🔗"
      url: "https://luckperms.net/wiki/Verbose"
    }]
  }
}
