{
  keywords: ["contexts"]
  displayName: "🔍 contexts"

  message {
    summary: "Información sobre los Contexts"
    embeds: [{
      include "../__plantillas__/embed.conf"
      description: """
Los Contexts (Contextos) permiten que un permiso solo se aplique en determinadas situaciones. Por ejemplo, que un jugador solo tenga un rango en un mundo.

Para añadir uno, necesitas su `key` (nombre), y el `value` (valor) que le quieres dar. Por ejemplo:
* El contexto `world` hace que el permiso solo se aplique en el mundo que pongas de `value`.
* El `gamemode` hace que solo se aplique para jugadores en el gamemode de `value`.
[Puedes ver la lista de Contextos por defecto en la Wiki.](<https://luckperms.net/wiki/Context#contexts-provided-by-luckperms>) Otros plugins pueden añadir más Contextos custom.

Una vez sepas lo que quieres añadir, lo puedes hacer desde el editor de LuckPerms, como se ve en la imagen.
"""
      image: "https://luckperms.net/img/context-1.423b1409.gif"
      footer {
        text: "Nota: Para usar el context server, necesitas primero establecer el nombre de cada servidor en sus config.yml."
      }
    }]
    buttons: [{
      type: "url"
      label: "Ver en la wiki"
      emoji: "🔗"
      url: "https://luckperms.net/wiki/Context"
    }]
  }
}
