{
  keywords: ["prevent_join"]
  displayName: "⛔ prevent_join"

  message {
    summary: "Cancela la entrada del jugador con un mensaje. (prevent_join)."
    embeds: [{
      include "../../__plantillas__/embed.conf"
      title: "`⛔` Acción `prevent_join`"
      url: "https://ajneb97.gitbook.io/conditionalevents/actions/event-actions#prevent-join"
      description: """
> **__Disponible solo para evento `player_pre_join`__**
Cancela la entrada del jugador con un mensaje.
```yaml
# Uso: "prevent_join: <mensaje>".
- "prevent_join: No puedes entrar a esta cuenta."
```
"""
    }]
    buttons: [{
      type: "tag"
      label: "Evento player_pre_join"
      style: "Secondary"
      emoji: "🧑"
      tag {
        resource: "conditionalevents"
        category: "eventos"
        tag: "player_pre_join"
      }
    }]
  }
}
