{
  keywords: ["lista", "condiciones"]
  displayName: "📄 lista"

  message {
    summary: "Cómo hacer una condición que revisa si algo está en una lista."
    embeds: [{
      include "../__plantillas__/embed.conf"
      description: """
CE no tiene una forma directa de ver si algo está en una lista de valores, pero hay alternativas.
En este ejemplo revisaremos si `%placeholder%` está en la lista `A`, `B`, `C`.
```yaml
# Usando or
conditions:
  - "%placeholder% == A or %placeholder% == B or %placeholder% == C"

# Usando contains
conditions:
  - "A;B;C contains %placeholder%"
```
"""
    }]
  }
}
