{
  keywords: ["menos-plugins", "lag"]
  displayName: "🗒️ menos-plugins"
    
  message {
    summary: "Sobre el uso de CE para tener menos plugins."
    embeds: [{
      include "../__plantillas__/embed.conf"
      description: """
ConditionalEvents no está hecho para reemplazar plugins, si no para añadir funciones sencillas que puede que no hayas encontrado en un plugin individual.

Usarlo para reemplazar plugins no es eficiente, porque tiene que hacer más cosas que un plugin normal requeriría. Por ejemplo:
- Revisar si no es cancelado por permiso o `ignoreIfCancelled`.
- Para cada condición, parsear placeholders internos (variables) o externos (PlaceholderAPI), incluyendo lógica para encontrar y parsear posibles placeholders internos dentro de otros.
- Para cada acción, encontrar su tipo, y volver a correr los dos puntos anteriores para parsear placeholders.
- Una vez se parsearon placeholders y variables, extraer las opciones de la acción para poder ser usadas.
Todo ello un plugin lo puede hacer directamente, ya que ya está programado para eso.
"""
    }]
  }
}
