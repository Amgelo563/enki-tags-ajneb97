{
  keywords: ["server_stop"]
  displayName: "💻 server_stop"

  message {
    summary: "Cuando el servidor para."
    embeds: [{
      include "../../__plantillas__/embed.conf"
      title: "`👀` Evento `server_stop`"
      url: "https://ajneb97.gitbook.io/conditionalevents/event-types/other-events#server-stop"
      description: """
Ejecutado cuando el servidor se para.
```yaml
ejemplo:
  type: server_start
  actions:
    default:
    - "to_all: message: El servidor se está parando. Volvemos pronto!"
```
"""
    }]
  }
}
