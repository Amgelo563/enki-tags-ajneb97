{
  keywords: ["server_start"]
  displayName: "💻 server_start"

  message {
    summary: "Cuando el servidor inicia."
    embeds: [{
      include "../../__plantillas__/embed.conf"
      title: "`👀` Evento `server_start`"
      url: "https://ajneb97.gitbook.io/conditionalevents/event-types/other-events#server-start"
      description: """
Ejecutado cuando el servidor inicia.
```yaml
ejemplo:
  type: server_start
  actions:
    default:
    - "console_message: El servidor ha iniciado."
```
"""
    }]
  }
}
